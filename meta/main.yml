---
galaxy_info:
  author: your name
  description: 
  company: Codethink
  license: GPLv2
  min_ansible_version: 1.9
  - name: Ubuntu
    versions:
    - trusty
  categories:
  - system
  - web
  galaxy_tags:
  - storyboard
  - openstack
dependencies:
- { role: Mayeu.RabbitMQ, when: rabbitmq_host == 'localhost' }
- { role: geerlingguy.mysql, when: mysql_host == 'localhost' }
